{
  "name": "@chat-app/shared",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    "./prisma": "./dist/prisma.js",
    "./validation": "./dist/validation.js",
    "./variables": "./dist/variables.js"
  },
  "scripts": {
    "build": "prisma migrate dev && prisma generate && tsc",
    "build:code": "tsc",
    "build:db": "prisma migrate dev && prisma generate",
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "studio": "prisma studio --browser none"
  },
  "devDependencies": {
    "@types/node": "^24.12.4",
    "prisma": "^7.8.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "dotenv": "^17.4.2",
    "zod": "^4.4.3"
  }
}
